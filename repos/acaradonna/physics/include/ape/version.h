#pragma once

/**
 * @file ape/version.h
 * @brief Engine semantic version macros.
 *
 * Keep these values in sync with the CMake project version to ensure the C ABI
 * reports a coherent version via `ape_version_*()`.
 */
#define APE_VERSION_MAJOR 0
#define APE_VERSION_MINOR 0
#define APE_VERSION_PATCH 1
